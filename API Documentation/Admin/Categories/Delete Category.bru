meta {
  name: Delete Category
  type: http
  seq: 3
}

delete {
  url: {{base_url}}/admin/categories/:categoryName
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  x-language: eng
}

params:path {
  categoryName: CategoryToDelete
}
