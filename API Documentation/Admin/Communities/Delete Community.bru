meta {
  name: Delete Community
  type: http
  seq: 8
}

delete {
  url: {{base_url}}/admin/communities/:communityId
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  x-language: eng
}

params:path {
  communityId: community_id_here
}
