meta {
  name: Get User Detail
  type: http
  seq: 2
}

get {
  url: {{base_url}}/admin/users/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  x-language: eng
}

params:path {
  id: user_id_here
}
