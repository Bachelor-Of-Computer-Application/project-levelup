meta {
  name: Delete User
  type: http
  seq: 4
}

delete {
  url: {{base_url}}/admin/users/delete
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Content-Type: application/json
  x-language: eng
}

body:json {
  {
    "id": "user_id_here"
  }
}
