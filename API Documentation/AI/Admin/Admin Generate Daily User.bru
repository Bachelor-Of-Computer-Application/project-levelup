meta {
  name: Admin Generate Daily User
  type: http
  seq: 2
}

post {
  url: {{base_url}}/ai/admin/generate/daily/:userId
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  x-language: eng
}

params:path {
  userId: user_id_here
}
