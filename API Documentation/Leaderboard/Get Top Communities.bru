meta {
  name: Get Top Communities
  type: http
  seq: 3
}

get {
  url: {{base_url}}/leaderboard/top-communities?sortBy=xp&order=desc&limit=20
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  x-language: eng
}

params:query {
  ~sortBy: xp
  ~order: desc
  ~limit: 20
}
